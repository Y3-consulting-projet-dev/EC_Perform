# Laissé vide : l'app déduit l'hôte de l'API depuis celui utilisé pour charger la page
# (voir src/utils/apiUrl.js), pour fonctionner aussi bien via localhost que via l'IP du
# poste sur le réseau local. Ne définir VITE_API_URL que pour pointer vers une autre API.
